# Estado actual — Horma App
> Actualizar al terminar cada sesión de trabajo en este proyecto

## Última actualización: 26/08/2026 (continuación 4)

## Sesión 26/08/2026 (continuación 4) — Ajustes manuales, adelantos con comprobante, Historial de pagos, Archivar en Gustavo — siguiendo el plan aprobado (`steady-purring-spring.md`)

Implementado en el orden del plan, `tsc --noEmit` (desde `src/`, comando del arnés) limpio después de cada paso y al final. Sin `git push` (no se pidió). Resuelve los dos casos reales que trajo Alexandra: Fabriel trabajando un sábado (comprobante $100.000 vs. calculado $50.000) y Henry/Alejandro cobrando de menos por una corrección de semana anterior.

1. **Ajustes manuales por fila — construido, bloqueado en una migración pendiente.** `sql/20260826_ajustes_pago_semanal.sql` (tabla `ajustes_pago_semanal`, mismo patrón anon-full-access de siempre). **Confirmado contra Supabase real (MCP, solo lectura) antes de escribir código que la tabla no existía.** En `PanelPagoSemanal` cada fila tiene un botón "▾ Ajustar/Adelanto" que expande una fila de detalle con "± Ajustar" (monto +/-, motivo) → insert con `semana_key` de la semana mostrada. El Neto de la fila ahora es `Calculado + Ajustes` (antes de restar adelantos), con los motivos listados en el detalle expandible.

2. **Adelantos con comprobante — construido, bloqueado en la misma migración pendiente.** `sql/20260826_adelantos_trabajador.sql` (tabla `adelantos_trabajador`). Botón "+ Adelanto" en la misma fila de detalle (monto, fecha default hoy, nota, subir comprobante — mismo patrón que `ComprobanteCelda`/`subirFotoBoleta`, bucket `audio-notas`) → insert. **Cómo resta, verificado en el código y en el navegador:** para trabajadores semanales (`sueldoFijo=false`, todos salvo Fabriel), sus adelantos de esa semana (por rango de fecha lunes-domingo, función `semanaRango`) restan del Neto: `Neto = Calculado + Ajustes − Adelantos`. Para Fabriel (`sueldoFijo=true`), sus adelantos NO restan de la fila semanal — la UI muestra la nota "Sus adelantos no restan acá (sueldo fijo)" y se acumulan aparte para el historial mensual (punto 3). `ComprobanteCelda` ahora compara el comprobante subido contra el **Neto** (antes comparaba contra `f.total` bruto) — se le pasa `montoCalculado={f.neto}` en vez de `f.total`.
   - Cálculo factorizado en una función compartida nueva, exportada para reuso: `calcularFilaPagoSemanal()` + `semanaRango()`, ambas en `PanelesObra.tsx` — la usan tanto `PanelPagoSemanal` (semana en curso, con formularios) como `PanelHistorialPagos` (semanas pasadas, solo lectura), para que el Neto nunca se calcule distinto en los dos lugares.

3. **"Historial de pagos" — componente nuevo `PanelHistorialPagos`, card separada de "Pago semanal" en Admin y Gustavo, construido y verificado.** Selector de trabajador (tabla `trabajadores`).
   - **Sueldo fijo (Fabriel):** agrupa por mes calendario (mes de cada semana según el lunes). Trae `gastos_fijos` con `concepto` conteniendo el nombre del trabajador (case-insensitive) y `activo=true` = Sueldo del mes; trae `adelantos_trabajador` de ese trabajador agrupados por mes = Adelantado; `Resta pagar = Sueldo − Adelantado`; debajo, detalle semanal de viático/ajustes/comprobante de ese mes (reusa `calcularFilaPagoSemanal` con adelantos vacíos, ya que para sueldo fijo no restan de la semana).
   - **Semanal (resto):** agrupa por semana, reusando `calcularFilaPagoSemanal` — por semana: Calculado, Ajustes (con motivo), Adelantos (con comprobante), Neto, Comprobante final (mismo `ComprobanteCelda`, permite subir/reemplazar también desde el historial).
   - Card "Historial de pagos" agregada en `Gustavo.tsx` y `Admin.tsx` (mismo criterio que "Pago semanal").

4. **Archivar clientes también en Gustavo — hecho y verificado.** En `PanelClientes` (`PanelesObra.tsx`) se sacó el `modoAdmin &&` que tapaba el botón Archivar/Desarchivar y el toggle "Ver archivados" — ahora aparecen en los dos paneles. "+ Pendiente" se dejó intacto, sigue dependiendo solo de que `onNuevoPendiente` esté definido (Gustavo no lo pasa, así que no aparece ahí — verificado).

**Verificación hecha, con qué se comparó:**
- `tsc --noEmit` limpio después de cada paso y al final (sin errores).
- **Contra Supabase real (MCP `supabase-horma`, solo lectura), antes de escribir las migraciones:** confirmado que ni `ajustes_pago_semanal` ni `adelantos_trabajador` existían; confirmado `trabajadores` (Fabriel `tarifa_diaria=0`, el resto con tarifa); confirmado `gastos_fijos` de Fabriel = "Sueldo fijo Fabriel" $700.000 + "Bono encargado de obra - Fabriel" $300.000 = **$1.000.000/mes exacto**.
- **Simulación de insert con la anon key (mismo canal que el frontend, vía `curl`) contra las dos tablas nuevas:** ambas fallan con `PGRST205 — Could not find the table`, el error exacto esperado — confirma que el código de guardado está listo y solo falta que Alexandra corra las 2 migraciones. Repetido al final de la sesión, mismo resultado (nada quedó insertado por error).
- **Probado en navegador real (Vite local + Playwright, cargado desde el npx cache ante la falta de instalación local):** `PanelPagoSemanal` en Gustavo muestra la semana en curso (24-30 ago) con Total neto **$480.000**, exactamente el mismo consolidado ya verificado en una sesión anterior contra Supabase (Fabriel/Henry/Manuel/Misael/Samuel) — confirma que la refactorización a `calcularFilaPagoSemanal` no cambió ningún número existente. Fila de Fabriel expandida: muestra "Ajustar/Adelanto", formulario "± Ajustar" abre con Monto/Motivo, formulario "+ Adelanto" abre con Monto/Fecha/Nota/Comprobante; ambos guardados fallan con el error de tabla esperado (ver arriba), sin romper la pantalla.
   - **Hallazgo real, no fabricado: `PanelHistorialPagos` para Fabriel ya muestra en vivo, con datos reales, el caso exacto que motivó esta tarea** — la semana 17-23 ago aparece con "⚠ No coincide: comprobante $100.000 · calculado $50.000" (el comprobante real que ya había subido Gustavo esa semana). En cuanto Alexandra corra la migración y cargue el ajuste de +$50.000 "trabajó sábado" para esa semana, el calculado pasará a $100.000 y va a coincidir solo — sin tocar nada más.
   - `PanelHistorialPagos` para Henry (semanal) muestra 4 semanas con Calculado/Neto idénticos a los ya verificados antes (semana en curso $120.000, etc.) — coincide con el consolidado ya confirmado contra Supabase.
   - `PanelClientes` en Gustavo: confirmado en pantalla que "Ver archivados" aparece en la lista y "Archivar" aparece en el detalle de un cliente real (Alfonso Vadell), y que "+ Pendiente" **no** aparece (correcto, sigue siendo exclusivo de Admin).
- **Sin datos de prueba insertados en ningún lado** — todos los intentos de guardar (ajuste, adelanto) fallaron por la migración pendiente, así que no hay nada que limpiar en Supabase ni en Storage (no se llegó a probar la subida de comprobante de un adelanto porque el insert final de todas formas iba a fallar).
- **Admin.tsx no se pudo probar en vivo** (login vía Cloudflare Function, límite conocido) — confirmado que la pantalla de login sigue cargando sin errores de consola tras el cambio; el resto se verificó por lectura de código y `tsc` limpio, apoyado en que reusa exactamente los mismos componentes (`PanelPagoSemanal`, `PanelHistorialPagos`, `PanelClientes`) ya verificados del lado de Gustavo.

**Pendiente para Alexandra — 2 migraciones nuevas a correr (no dependen una de la otra, cualquier orden):**
1. `sql/20260826_ajustes_pago_semanal.sql`
2. `sql/20260826_adelantos_trabajador.sql`

**Después de correr las 2 migraciones, repetir esta verificación (no se pudo hacer hoy porque dependía de ellas):**
- Cargar un ajuste real de prueba (+$50.000, motivo "trabajó sábado") sobre la fila de Fabriel semana 17-23 ago y confirmar en pantalla que el Neto sube a $100.000 y el badge pasa de "No coincide" a "✓ Coincide" — sería la primera vez que se ve el flujo completo funcionando con el caso real que lo motivó.
- Cargar un adelanto de prueba con comprobante a un trabajador semanal y confirmar que resta del Neto de esa semana; cargar uno a Fabriel y confirmar que NO resta de su fila semanal pero sí aparece en su "Historial de pagos" contra el sueldo mensual.
- Borrar cualquier dato de prueba real que se cargue en el camino, sin dejar rastro (mismo criterio de siempre).

## Sesión 26/08/2026 (continuación 3) — Banco de contenido + Ideas de contenido, siguiendo el plan aprobado (`steady-purring-spring.md`)

Implementado en el orden del plan del Estratega, sin rediseñar nada. `tsc --noEmit` (desde `src/`, comando del arnés) quedó limpio después de cada paso y al final. Commit local hecho (`7115626`), **sin push** (no se pidió).

1. **`obra_media` extendida — migración escrita, bloqueada en que Alexandra la corra.** `sql/20260826_obra_media_contenido.sql`: agrega `lat numeric`, `lng numeric`, `momento text check(in antes/durante/despues)`, `autorizado_cliente boolean default false`, `destacado boolean default false`. Todo nullable o con default — no rompe los inserts existentes de `GaleriaObra`. **Verificado antes de escribir el código que hacía falta**: `select column_name from information_schema.columns where table_name='obra_media'` (vía MCP `supabase-horma`, solo lectura) confirmó que ninguna de las 5 columnas existe todavía. **Simulado el insert con la anon key (mismo canal que el frontend) antes de dar el código por bueno**: falló con `PGRST204 — Could not find the 'autorizado_cliente' column`, el error exacto esperado — confirma que el código está listo y solo falta la migración.

2. **Ruta `/obra-fotos` para Fabriel y Misael — construida y verificada de punta a punta en navegador (Vite local + Playwright), salvo el insert final que depende de la migración de arriba.**
   - Dos tokens nuevos generados (hex de 16, mismo formato que los demás) y agregados a `.env` (`VITE_FABRIEL_TOKEN=55165640daf27c94`, `VITE_MISAEL_TOKEN=55cecd957fcf3736`) y a `.env.example` (sin valores). **Confirmado contra Supabase que los nombres `Fabriel`/`Misael` existen tal cual en `trabajadores`** antes de usarlos.
   - `src/pages/ObraFotos.tsx` nuevo, ruta en `src/App.tsx`. Resuelve el trabajador por token (mismo patrón que `/g`/`/reporte`), pantalla "Link inválido" si no coincide. Formulario de una sola pantalla: selección de obra (`estado_obra='en_curso'` — confirmado contra Supabase que hoy son 4 obras reales), momento (Antes/Durante/Después), checkbox de autorización del cliente, input de archivo múltiple. Al subir pide geolocalización con `navigator.geolocation.getCurrentPosition` con timeout de 7s, resuelve a `null` si se niega/no está disponible/tarda — nunca bloquea la subida. Sube cada archivo al bucket `audio-notas` (mismo patrón que `GaleriaObra`) e inserta en `obra_media`.
   - **Probado en navegador real (Vite local + Playwright headless, Chromium ya instalado en el entorno):** token inválido bloquea con el mensaje esperado; con el token de Fabriel y geolocalización simulada (`context.geolocation`), se pudo elegir obra, momento "Antes", marcar autorización y subir un archivo de prueba — la subida a Storage funcionó (confirmado listando el bucket), pero el insert en `obra_media` falló con el error esperado (columna no existe todavía) y la UI lo mostró como "1 fallaron, intenta de nuevo" sin romper la pantalla. Repetido con el token de Misael, momento "Después", sin geolocalización otorgada (contexto sin permisos) — misma degradación correcta.
   - **Geolocalización: en este entorno de prueba se pudo simular con el permiso de contexto de Playwright** (`geolocation: {...}`, `permissions: ['geolocation']`), pero como el insert real no llegó a completarse (migración pendiente), no se pudo confirmar todavía que `lat`/`lng` lleguen escritos en una fila real de `obra_media` — queda para repetir la prueba después de la migración.
   - **Limpieza:** los 2 archivos de prueba subidos a Storage (68 bytes cada uno, PNG de prueba) no se pudieron borrar con la anon key (el bucket no tiene policy de DELETE para `anon` — mismo límite que ya tiene `GaleriaObra`, que tampoco borra el archivo de Storage al eliminar la fila de `obra_media`, solo la fila). Quedan 2 archivos huérfanos triviales en `audio-notas` con nombre `obra-0ca23eb4-...-*.png` — inofensivos, pero quedan anotados para que Alexandra los borre a mano desde el dashboard de Supabase si quiere prolijidad, o para que alguien revise en algún momento si vale la pena una policy de DELETE para `anon` en el bucket.

3. **`PanelBancoContenido` (vista de Alexandra) — construido, agregado como card nueva solo en Admin (así lo pedía el plan). Probado en navegador montándolo temporalmente en Gustavo (única forma de probarlo en vivo, ver nota abajo), no queda ningún rastro de esa prueba en el código.**
   - Filtros: obra (desplegable "Todas" + cada obra, no solo en curso), vista Semana/Mes (mismo estilo de botones que otros paneles), selector de período dentro de la vista elegida, checkbox "Solo destacados". Grilla de miniaturas con: estrella clickeable (toggle `destacado` con `update` directo), ícono de check si `autorizado_cliente`, nombre de la obra (si el filtro es "Todas"), momento y quién lo subió, link "Descargar".
   - **Cómo se probó, siendo honesto sobre el método:** el plan pedía probarlo "del lado de Gustavo" pero el plan de arriba solo le da la card a Admin (no a Gustavo) — no hay contradicción real, es que Admin no se puede probar en navegador (login por Cloudflare Function). Para no dejar esto sin ejercitar en el navegador, se agregó **temporalmente** una card de prueba en `Gustavo.tsx`, se insertó una fila de prueba en `obra_media` (con las columnas viejas únicamente, ya que la migración nueva no está corrida), se navegó con Playwright, se confirmó que la grilla renderiza (obra, ícono, botón Descargar, estrella clickeable sin crashear aunque el toggle falle por la migración pendiente), se sacó captura, y **se revirtió la card de prueba del todo** (confirmado con `git diff` que `Gustavo.tsx` solo conserva el cambio real de "Ideas de contenido", nada de la card de prueba) y se borró la fila de prueba de Supabase. `tsc` limpio después de revertir.
   - Admin.tsx en sí **no se pudo probar en vivo** (límite de siempre) — se confirmó que la pantalla de login sigue cargando sin errores de consola tras el cambio, y que usa el mismo `PanelBancoContenido` ya ejercitado del lado de Gustavo.

4. **Ideas de contenido — migración escrita (bloqueada) + `PanelIdeasContenido` compartido, construido y probado del lado de Gustavo.**
   - `sql/20260826_ideas_contenido.sql`: tabla `ideas_contenido` nueva, mismo patrón `anon full access` de siempre. **Confirmado antes contra Supabase que la tabla no existe** (`information_schema.tables`). Simulado el insert con la anon key: falló con `PGRST205 — Could not find the table 'public.ideas_contenido'`, el error exacto esperado.
   - `PanelIdeasContenido({ soloLectura })` en `PanelesObra.tsx`: en Admin (`soloLectura` sin setear) formulario de creación (título/hook/formato/tema) + lista con borrar y toggle de estado; en Gustavo (`soloLectura`) misma lista, solo puede tildar "Marcar como hecho"/"Reabrir", sin crear/editar/borrar. Card "Ideas de contenido" agregada en ambos paneles.
   - **Probado en navegador del lado de Gustavo (Vite local + Playwright):** con la tabla todavía sin existir, el panel se degrada bien — no crashea, muestra "Todavía no hay ideas de contenido cargadas" (el error de Supabase se traga sin romper la pantalla, igual que otros paneles de la app en este mismo estado de "migración pendiente"). No se pudo probar el flujo de creación/marcado real porque depende de la migración.
   - Admin.tsx no se pudo probar en vivo (mismo límite de siempre) — usa el mismo componente ya verificado del lado de Gustavo.

**Pendiente para Alexandra — 2 migraciones a correr (no dependen una de la otra, cualquier orden):**
1. `sql/20260826_obra_media_contenido.sql`
2. `sql/20260826_ideas_contenido.sql`

**Pendiente que solo puede hacer Alexandra — Cloudflare Pages:**
- Agregar `VITE_FABRIEL_TOKEN=55165640daf27c94` y `VITE_MISAEL_TOKEN=55cecd957fcf3736` en Cloudflare Pages → proyecto `horma-presupuestos` → Settings → Environment variables → Production (mismo paso que ya se hizo, y que en su momento quedó pendiente, para `VITE_PRESUPUESTO_TOKEN`) → disparar un redeploy. **Sin esto, `/obra-fotos` no va a funcionar en producción aunque el código esté desplegado** — el bug de `VITE_PRESUPUESTO_TOKEN` de la sesión anterior es la prueba de que agregar la variable sola, sin redeploy, no alcanza.
- Los links reales para mandarle a Fabriel y Misael por WhatsApp (una vez que la variable esté en Cloudflare y el redeploy se haya hecho):
  - Fabriel: `https://horma-presupuestos.pages.dev/obra-fotos?t=55165640daf27c94`
  - Misael: `https://horma-presupuestos.pages.dev/obra-fotos?t=55cecd957fcf3736`

**Después de correr las 2 migraciones, queda pendiente repetir estas verificaciones (no se pudieron hacer hoy porque dependían de las migraciones):**
- Insert real completo desde `/obra-fotos` (Storage + `obra_media` con `lat/lng/momento/autorizado_cliente` de verdad) — confirmar contra Supabase que la fila queda bien formada, con especial atención a si `lat`/`lng` quedan null en un celular real (la geolocalización del navegador puede pedir permiso la primera vez).
- Toggle de `destacado` en `PanelBancoContenido` con la columna ya existente (hoy el botón no crashea pero tampoco persiste, por la migración pendiente).
- Crear/borrar/marcar como hecho una idea real en `PanelIdeasContenido` desde Admin, y confirmar que Gustavo la ve y puede marcarla como hecha (no crear/borrar) desde su lado.
- Limpiar a mano (opcional) los 2 archivos huérfanos de prueba en el bucket `audio-notas` (`obra-0ca23eb4-30a3-48fc-9be7-dab4c1923522-1787760405977-0.png` y el `...-1787760412424-0.png`), o considerar si vale la pena una policy de `DELETE` para `anon` en ese bucket — hoy ni `GaleriaObra` ni esta subida pueden borrar el archivo de Storage al borrar la fila de la base.


## Sesión 25/08/2026 (continuación 2) — Nivel 1 completo salvo el paso final de 1.4

- **1.3 verificado end-to-end** después de que Alexandra corrió `sql/notas_rapidas_gustavo.sql`: columna `autor` + RLS + policy confirmados vía MCP, probado en navegador con Playwright (pestaña "Mis notas" de Gustavo: nota creada, marcada como hecha, sin errores), fila real en Supabase coincide exacto. Admin.tsx no se pudo probar en vivo (login vía Cloudflare Function, límite conocido) pero es el mismo componente ya verificado del lado de Gustavo.
- **1.4 construido** (`cliente_id` real en `pendientes`): `sql/pendientes_cliente_id.sql` nuevo, `Admin.tsx` (`CrearForm.submit`, único insert a `pendientes`) hace upsert de cliente por nombre antes de guardar, mismo patrón que 1.1/1.3. Sin migrar historial viejo, como decía el plan. `tsc`/`init.sh` en verde. **No se pudo probar en navegador** (mismo límite de Admin.tsx) — falta que Alexandra corra el SQL y pruebe creando un pendiente real; se puede confirmar después contra Supabase vía MCP.
- **1.4 cerrado del todo, con un bug real encontrado y arreglado en el camino.** Como Admin.tsx no se puede probar en navegador (login vía Cloudflare Function), se verificó simulando el flujo del código directo contra Supabase (mismo canal, anon key). Primera simulación: el upsert de `clientes` funcionó, pero el `insert` en `pendientes` falló — el constraint `pendientes_tipo_check` nunca se había actualizado cuando se agregaron los tipos `seguimiento`/`pedido_material` esta sesión (24-25/08), así que crear un pendiente de esos dos tipos estaba roto en producción (nadie lo había pisado todavía, último pendiente real es de mayo). Fix corrido por Alexandra (`sql/20260825_pendientes_tipo_constraint_fix.sql`). Segunda simulación, con `tipo='pedido_material'` (el caso que fallaba): pasó limpio, cliente creado, `cliente_id` vinculado, pendiente insertado. Datos de prueba borrados. **Nivel 1 completo.**
- **Convención de nombres para `sql/*.sql` acordada con Alexandra** (pidió un sistema de orden): de acá en adelante, `YYYYMMDD_descripcion.sql` en el repo, y el mismo nombre (sin `.sql`) como nombre de la consulta guardada en el SQL Editor de Supabase. Documentado en `decisiones.md`. Los archivos viejos no se renombraron (ya corridos, sería solo cosmético).
- **Fix chico pedido por Alexandra: Gustavo faltaba en el dropdown "Quién lo hizo" de trabajos puntuales.** No se agregó a la lista `TRABAJADORES` general (esa alimenta asistencia diaria y pago semanal — Gustavo no tiene tarifa diaria, no está en la tabla `trabajadores`, cobra distinto por ser el dueño; agregarlo ahí lo habría metido por error en cálculos de payroll). Se creó `QUIEN_LO_HIZO` aparte en `Reporte.tsx`, solo para ese dropdown. Verificado en navegador: Gustavo aparece ahí, no en la asistencia. `tsc`/`init.sh` en verde.
- **Arrancado Nivel 2 con 2.1 ("Mis presupuestos"), construido, bloqueado en Alexandra corriendo `sql/20260825_presupuestos_estado.sql`.** Columna `estado` nueva en `presupuestos`; los dos flujos de guardado (simple y por etapas) ponen `estado:'enviado'` al generar el PDF (no existe borrador previo hoy). De paso, `PresupuestoEtapas.tsx` también resuelve `cliente_id` ahora (mismo upsert que 1.1, no estaba cubierto porque es un flujo aparte). Componente compartido `PanelPresupuestos` nuevo, con pestaña en Gustavo y en Admin. Probado en navegador antes de la migración: se degrada bien, sin romper. `tsc`/`init.sh` en verde.
- **2.2 saltada por decisión de Alexandra** (depende de `obra_items`, Nivel 3 — se retoma junto con 3.2, cuando la IA tenga dónde escribir de verdad).
- **2.3 y 2.4 construidas juntas, bloqueadas en 2 migraciones nuevas.** 2.3: "Nueva obra" exige elegir un presupuesto Aceptado (autocompleta cliente/monto, vincula por FK, marca el presupuesto como convertido) — con salida de emergencia ("Crear sin presupuesto") que Alexandra pidió explícitamente en vez del gate estricto. 2.4: `activa` (booleano de siempre) pasó a ser columna **generada** desde el nuevo `estado_obra` (5 valores) — nunca se desincronizan, todo el código viejo que lee `.activa` sigue andando sin tocarlo. Selector de 5 estados + 3 campos de fecha nuevos en cada tarjeta de obra. Backfill: las 3 obras `activa=false` de hoy quedan en `'cerrada'` genérico, pendiente que Alexandra las ajuste si corresponde un estado más específico. `tsc`/`init.sh` en verde, probado en navegador antes de migrar (carga sin errores). Falta correr `sql/20260825_obras_presupuesto_id.sql` y `sql/20260825_obras_estado_ampliado.sql`.
- Con eso, **Nivel 2 completo salvo 2.2** (pospuesta a propósito). Sigue 2.5 (galería de fotos/videos) y 2.6 (calendario compartido), propuestas de Alexandra sin construir.
- **Deploy real de todo lo anterior:** hasta acá, todo el trabajo de la sesión quedaba solo local — Alexandra preguntó por qué no lo veía en producción. Committeado y pusheado (`5f73559`), Cloudflare Pages lo tomó automático.
- **Extras pedidos después de ver la pestaña en vivo:** botón "Detalle" en Mis presupuestos (modal con desglose real, ítems o etapas) y botón "Borrar" (con confirmación) — ambos construidos, verificados de punta a punta, y pusheados (`fe6021b`, `066e2e5`). De paso se encontraron 4 presupuestos duplicados de "Patricio" en los datos reales (de mayo, pre-existentes) — Alexandra los puede borrar ahora ella misma con el botón nuevo.
- **2.5 (galería de fotos/videos por obra) construida, bloqueada en `sql/20260825_obra_media.sql`.** Tabla nueva `obra_media`, reusa el bucket de Storage que ya existe. Sección "Fotos y videos" nueva arriba del todo en el modal "Detalle" de cada obra (Gustavo y Admin, es el mismo modal compartido) — subir, ver en grilla, borrar. Alcance: solo por obra, no por cliente (una galería a nivel cliente queda para después si hace falta); no se conectó a Reporte Diario todavía (fast-follow si Gustavo lo pide). `tsc`/`init.sh` en verde, probado en navegador antes de migrar (carga sin errores, se degrada bien).
- **Corrección de lenguaje:** Alexandra pidió español neutro y profesional, sin voseo ni modismos — corregido en el chat y en el texto de la interfaz que se había escrito con voseo hoy (memoria guardada para no repetirlo).
- **Visión ampliada de Alexandra (7 ideas nuevas) documentada en `propuesta_arquitectura_operativa.md`** como 2.7 a 2.13, sin construir todavía salvo lo que sigue. Idea general: "un cliente debe vivir organizado toda su vida dentro de su creación" (presupuesto→pago→factura→obra→garantía).
- **2.7+2.8 construidas** (media a nivel cliente + galería navegable en pendientes, elegidas por Alexandra como primera prioridad): componente nuevo `GaleriaArchivos`, reemplaza la lista de "Archivo 1, Archivo 2..." en 5 lugares (Gustavo.tsx, Admin.tsx) por miniaturas con visor de pantalla completa. Sin tabla nueva — era un problema de presentación, no de datos. Soporte especial para links de Drive pegados a mano (como los de "Rolando | Recoleta", el ejemplo real que dio Alexandra): se piden como miniatura pública de Drive, con "Ver original en Drive" de respaldo si el endpoint de Google falla (le pasó durante las pruebas, pedirle el mismo archivo muchas veces seguidas lo vuelve inestable — no es un bug del código). Los archivos subidos de acá en adelante por la propia app no tienen ese límite. `tsc`/`init.sh` en verde, probado con el pendiente real de Rolando.
- **2.3, 2.4 y 2.5 verificadas de punta a punta 25/08/2026**, tras confirmar contra Supabase (no contra lo anotado) que las tres migraciones estaban corridas — se habían quedado pendientes desde antes, Alexandra las corrió recién ahora. Probado con datos de prueba (presupuesto+obra+archivo, todos borrados después, sin dejar rastro): crear obra desde presupuesto aceptado resuelve cliente/monto/FK y marca el presupuesto como convertido; cambiar el estado de una obra recalcula `activa` sola (columna generada); subir un archivo a la galería de una obra funciona y se borra en cascada si se borra la obra.
- **2.6 (calendario compartido) verificada de punta a punta 25/08/2026.** Versión liviana (persona = nombre de una lista fija, no login real, como se había acordado). Se probó el caso central de la tarea: agendar dos eventos superpuestos para la misma persona — el sistema avisó del choque exacto antes de guardar el segundo, y guardó al confirmar. Datos de prueba borrados sin dejar rastro. **Nivel 2 completo.**
- **2.14 nueva (pedido de Alexandra usando la app en vivo): "Agregar compra" solo ofrecía obras reales.** Agregadas dos opciones al desplegable: "Stock (sin obra todavía)" y "Trabajo puntual (sin obra)" — columna nueva `destino` en `reportes_compras`, sin tocar `obra` (sigue significando lo mismo de siempre, ningún cálculo existente se ve afectado). Alcance a propósito chico: es solo la categorización, no el catálogo de stock real ni la IA que lee la boleta — eso es Nivel 3 (3.1/3.3), que Alexandra ya había anticipado correctamente. Bloqueada en `sql/20260825_compras_destino.sql`. `tsc`/`init.sh` en verde, probado en navegador antes de migrar (aparecen las dos opciones, sin errores).
- **Corrección de emojis:** Alexandra marcó que agregué emojis nuevos sin que se pidieran (📦🔧 en compras, 🎬📄 en las galerías, 📞✉️📍 en detalle de presupuesto) — corregidos a texto plano, memoria guardada para no repetirlo. Los emojis preexistentes del proyecto no se tocaron.
- **2.9 construida** (referencia en presupuestos por etapas): mismo formato `HRM-XXXXX` que ya usaba el presupuesto simple, impresa en el PDF y guardada en Supabase. Sin migración (columna ya existía). **No se pudo probar en navegador** — `/itemizado` exige login real de Supabase Auth sin credenciales disponibles acá; falta que Gustavo o Alexandra lo prueben una vez real.
- **2.11 construida y verificada de punta a punta** (botón "Convertir en obra" en la tarjeta y en el detalle de un presupuesto "Aceptado"): formulario chico inline, solo pide el nombre de la obra (precompletado con la dirección del cliente). Sin migración — reusa lo de 2.3. Probado con presupuesto de prueba, borrado después.
- **2.13 construida** (tipo de pendiente "Solicitud de garantía"): esta vez el constraint de la DB se actualiza en la misma migración que el código, justo para no repetir el bug real de la última vez. Bloqueada en `sql/20260825_pendientes_solicitud_garantia.sql`. `tsc`/`init.sh` en verde.
- **2.12 construida** (comprobante de pago en abonos): botón "+ Comprobante" en el formulario de agregar abono, sube al mismo bucket de siempre, cada abono muestra "Ver comprobante" si tiene uno. Probado con cuenta de prueba: la subida funciona sola (no depende de la migración), guardar el abono falla con el aviso correcto hasta que se corra `sql/20260825_abonos_comprobante.sql`. Bloqueada en esa migración.
- **2.10 construida** (hilo de mensajes por pendiente, confirmado con Alexandra que lo quería tipo chat): tabla nueva `pendiente_mensajes`, sin tocar `respuesta`/`estado` (el mecanismo de cierre ya en uso a diario, para no arriesgar romperlo). Pestaña "Hilo" en la tarjeta de Gustavo, sección persistente en la de Admin — permite ida y vuelta sin cerrar el pendiente. Solo en las tarjetas activas, no en las vistas de historial de solo lectura (posible fast-follow). Bloqueada en `sql/20260825_pendiente_mensajes.sql`. Probado en navegador (se degrada bien).
- **2.13 y 2.12 verificados de punta a punta 25/08/2026** (Alexandra corrió las migraciones, confirmado contra Supabase real): pendiente con `tipo='solicitud_garantia'` insertado sin error; abono con `comprobante_url` real guardado sin error. Datos de prueba borrados. **2.13 y 2.12 cerrados del todo.**
- **2.10 verificado de punta a punta 25/08/2026** (después de reenviar `sql/20260825_pendiente_mensajes.sql`, que no se había corrido la primera vez pese a que Alexandra dijo que sí): mensaje real enviado desde la pestaña "Hilo", confirmado en Supabase, borrado en cascada al borrar el pendiente de prueba. **2.10 cerrado del todo.**
- **Con esto, toda la "visión ampliada" (2.7-2.13) queda construida y verificada de punta a punta contra Supabase real.**
- **Arrancado Nivel 3, paso 1 de 4 (plan acordado con Alexandra):** IA lee la foto de la boleta y completa descripción+monto de una compra (sin itemizar todavía — eso es el paso 2). Función nueva `functions/api/parse-factura.js` (mismo patrón que `parse.js`).
- **Alexandra corrió `sql/20260825_compras_foto_boleta.sql` y probó con una boleta real en su celular, en producción — funciona.** La IA leyó el monto exacto ($33.840) y los materiales de una boleta real. Confirma que la función de Cloudflare y OpenAI andan bien — era la única pieza no probable en local, ya validada con caso real.
- **Hallazgo real, esperado:** con varios materiales en una boleta, la descripción los junta en una sola línea de texto — no se ven cantidades ni precios por ítem. Confirma que **el paso 2 (desglose por ítem, tabla nueva `compra_items`) hay que priorizarlo pronto** en la próxima sesión — no era solo una posibilidad a futuro, ya se sintió la limitación en el primer uso real.

- **Nivel 3, paso 2 construido (desglose por ítem de la boleta):** tabla nueva `compra_items`, `functions/api/parse-factura.js` ahora también devuelve la lista de materiales con cantidad y precio unitario, sección "Materiales de esta compra" nueva en Reporte Diario (se llena sola con la IA, editable a mano). Guardado inserta cada compra primero para tener su ID real y recién después vincula sus materiales. Probado lo que se pudo probar sin riesgo: la interacción de la UI funciona, la escritura a `compra_items` se simuló contra Supabase (falló como se esperaba, migración no corrida). **No se probó a través del formulario real de Reporte Diario a propósito** — hubiera tocado la asistencia real de hoy del equipo (el guardado del día borra y reinserta todo). Bloqueado en `sql/20260825_compra_items.sql`.

- **Nivel 3, pasos 2 y 3 verificados de punta a punta 25/08/2026** (Alexandra corrió las migraciones): simulación completa contra Supabase real — entrada de stock desde una compra, salida por uso en obra, y el caso más delicado (reenviar el mismo día no duplica el stock, los triggers revierten y reaplican correcto). Todos los números coincidieron exacto con lo esperado. **Nivel 3: pasos 1, 2 y 3 de 4 cerrados.** Solo queda el paso 4 (alerta de material faltante), bloqueado hasta 2.2/3.2.
- **Mejora a 2.6, pedida por Alexandra: vistas Día/Semana/Mes en el calendario (como Google Calendar) + dirección clickable a Maps/Waze.** `PanelCalendario` reescrito: switcher de vista, navegación anterior/siguiente/Hoy, tres layouts (día, semana, grilla de mes con celdas clickeables). La dirección de cada evento ahora tiene links "Maps" y "Waze" con la dirección codificada en la URL — el usuario elige a cuál abrir, sin copiar nada. **Sin migración nueva**, es cambio de frontend sobre `eventos_calendario` que ya existía. Verificado con Playwright contra Vite local: evento de prueba visible en semana y día, vista mes carga sin error, `href` de Maps/Waze correctos, navegación sin errores de consola. Datos de prueba borrados. `tsc`/`init.sh` en verde.
- **Pestañas del panel de Gustavo reordenadas** a pedido de Alexandra por prioridad de uso diario: Reporte diario, Calendario, Obras, Pago semanal, Mis presupuestos, Mis tareas, Mis notas, y después Clientes/Stock/Estado de resultados. Solo el panel de Gustavo — Admin tiene sus propias pestañas, sin tocar. Verificado en navegador que el orden real coincide.
- **Bug real encontrado y arreglado: el link "Presupuesto" en la barra de navegación de Admin no llevaba el token.** Apuntaba a `/` sin `?t=...`, así que abrirlo desde ahí siempre mostraba "Link inválido" (el token en la URL es el único control de acceso, no hay login). **Preexistente desde el 15/08/2026, no de esta sesión** — recién se notó ahora porque Alexandra lo probó desde el celular vía WhatsApp. Fix: `href` ahora arma `/?t=${VITE_PRESUPUESTO_TOKEN}`. Verificado con Playwright: sin token muestra inválido, con el token carga el presupuestador completo. `tsc`/`init.sh` en verde.
- **Bug encontrado, sin poder arreglarlo desde el código: sigue mostrando "Link inválido" en producción incluso después del fix de arriba.** Diagnóstico completo comparando el bundle real de `https://horma-presupuestos.pages.dev/` contra un build local: el fix del `href` sí está desplegado, pero **la variable de entorno `VITE_PRESUPUESTO_TOKEN` no está configurada en Cloudflare Pages** — el sitio en producción la lee como `undefined`, así que el link queda `/?t=undefined` y la comparación contra el token real nunca puede pasar, ni siquiera pegando el token correcto a mano. Confirmado también que `VITE_GUSTAVO_TOKEN` y `VITE_REPORTE_TOKEN` sí están bien configurados en Cloudflare (por eso esos paneles funcionan) — es específico de `VITE_PRESUPUESTO_TOKEN` (y de `VITE_IRAZU_TOKEN`, pero esa ruta está desactivada a propósito, no urge). **Esto requiere acceso al dashboard de Cloudflare que no está disponible desde acá — es 100% acción de Alexandra, ver siguiente sección.**

## Sesión 26/08/2026 — Bloqueador de VITE_PRESUPUESTO_TOKEN cerrado, nuevas consultas de Alexandra

- **Confirmado contra el bundle real de producción** (`https://horma-presupuestos.pages.dev/assets/index-C1INDMCd.js`): el token `91f471c31d2b2c9b` aparece compilado ahí, así que Alexandra sí agregó `VITE_PRESUPUESTO_TOKEN` en Cloudflare Pages y el redeploy se hizo. **El link "Presupuesto" de Admin queda cerrado del todo**, mismo criterio de siempre (no confiar en la palabra, verificar contra el artefacto real).
- **Alexandra trajo 4 consultas nuevas**, sin construir todavía: (2) pestaña que liste las boletas/facturas cargadas por la IA (hoy los datos caen en `reportes_compras`+`compra_items` pero no hay ninguna vista que las liste); (3) rediseño de navegación — cards en vez de pestañas; (4) opción de cargar un presupuesto manual (hecho fuera de la app); (5) agregar el presupuestador a las cards del panel de Gustavo. Ver conversación para el detalle de la decisión.

### Sesión 26/08/2026 (continuación) — Las 4 consultas nuevas, construidas siguiendo el plan aprobado (`steady-purring-spring.md`)

Implementadas en el orden acordado, cada una probada antes de pasar a la siguiente. `tsc --noEmit` (desde `src/`, comando del arnés) quedó limpio después de cada paso y al final.

1. **Pestaña "Boletas" — construida y verificada de punta a punta contra Supabase real.** `PanelBoletas` nuevo en `src/components/PanelesObra.tsx`, lista todas las compras de `reportes_compras` (con o sin foto) agrupadas por mes, con `compra_items` expandibles si existen. Agregada como tab en `Gustavo.tsx` y `Admin.tsx`. Probado en navegador (Vite local + Playwright) contra datos reales de agosto: 21 compras, total `$1.950.502` — coincide exacto con `select sum(monto) from reportes_compras where date_trunc('month',fecha)='2026-08-01'` corrido contra Supabase. Sin errores de consola.

2. **Presupuesto externo — construido, bloqueado en una migración pendiente de Alexandra.** Antes de escribir la migración se confirmó contra Supabase real (no se adivinó) que el constraint se llama `presupuestos_tipo_check` — el mismo tipo de chequeo que evitó repetir el bug de `pendientes_tipo_check` de la sesión pasada. Nueva migración `sql/20260826_presupuestos_externo.sql`: agrega `'externo'` al CHECK de `tipo` + columna `archivo_url text`. En `PanelPresupuestos`: botón "+ Cargar presupuesto externo" con upsert de cliente por nombre, monto, estado y subida de archivo (mismo patrón que `subirFotoBoleta` de `Reporte.tsx`, bucket `audio-notas`). Tercera rama en el label de tipo y en el modal de detalle (reusa `GaleriaArchivos` para mostrar el archivo; el desglose Subtotal/IVA se oculta para este tipo porque no aplica, sólo se muestra el Total). **Verificado con una simulación directa contra Supabase (anon key, mismo canal que usa el frontend) antes de correr la migración: el insert falla con `PGRST204 — Could not find the 'archivo_url' column`, exactamente el error esperado** — confirma que el código está listo y sólo falta la migración. Probado también el flujo completo en el navegador (Gustavo → Mis presupuestos → Cargar presupuesto externo): el formulario funciona, y al guardar muestra el aviso de error esperado (falla silenciosa controlada, no rompe la pantalla). **Falta que Alexandra corra `sql/20260826_presupuestos_externo.sql` y que se repita la simulación después para cerrar esto del todo.**

3. **Presupuestador embebido en Gustavo — construido y verificado.** `PresupuestoEtapas.tsx` ahora acepta `embedded?: boolean` (default `false`): si es `true`, salta el gate de sesión de Supabase Auth y manda `user_id: null` al guardar (mismo criterio de confianza que ya usa el presupuesto simple). La ruta `/itemizado` (`App.tsx`) sigue llamando `<PresupuestoEtapas />` sin el prop, así que el login real sigue exigiéndose ahí sin cambios. Nueva tab "Hacer presupuesto" en Gustavo.tsx con selector Simple/Por etapas, renderiza `<Presupuesto token={PRESUPUESTO_TOKEN}/>` o `<PresupuestoEtapas embedded/>`. Verificado en navegador (Vite local + Playwright): ambos modos cargan sin gate de login y sin errores de consola. Como los endpoints de IA (`/api/parse-etapas`, `/api/parse-json`) son Cloudflare Functions que no corren bajo Vite plano, no se pudo generar un PDF real de etapas de punta a punta vía la UI — mismo límite conocido de siempre, no es nuevo de este cambio. Para cerrar la brecha se simuló el insert exacto que manda el modo embebido (`user_id: null`, sin `tipo` explícito) directo contra Supabase con la anon key: **insertó sin error, `tipo` quedó en `'etapas'` por el default de la columna, fila borrada después** — confirma que el guardado real funciona de punta a punta aunque la generación del PDF con IA no se haya podido ejercitar localmente.

4. **Rediseño de navegación a cards — construido en Gustavo.tsx y Admin.tsx.** Estado `seccion: Key | null` (antes `tab`): `null` muestra una grilla de cards (home), cualquier otro valor muestra esa sección a pantalla completa con botón "← Volver". Se reusó tal cual el mapeo sección→componente que ya existía en cada archivo, sólo cambió el disparador (card en vez de pestaña) y el layout. En Admin.tsx las cards fijas "Activos"/"Gustavo" pasaron a ser dos cards más de la grilla, conservando su número grande y sus sub-indicadores (vencidos / activos de Gustavo). Card "Mis tareas" (Gustavo) lleva el contador de pendientes como badge numérico. Sin emojis nuevos — los acentos de cada card son un color de borde superior, no un ícono. No se extrajo un componente "chrome" compartido entre los dos archivos (la duplicación no resultó grande, se mantuvo el código en cada archivo tal como pide el plan cuando no vale la pena forzarlo). **Gustavo.tsx verificado en navegador (Vite local + Playwright):** home con las 12 cards, click a "Mis tareas" y "Obras" abre la sección a pantalla completa con "← Volver" funcionando, sin errores de consola, datos reales de Obras cargando bien. **Admin.tsx no se pudo probar en navegador** (login vía Cloudflare Function, límite conocido) — confirmado que al menos la pantalla de login carga sin errores de consola tras el cambio; el resto se verificó por lectura de código y por `tsc --noEmit` limpio, apoyándose en que reusa exactamente los mismos componentes (`PanelBoletas`, `PanelPresupuestos`, etc.) ya verificados del lado de Gustavo.

**Pendiente para la próxima sesión / para Alexandra:**
- Correr `sql/20260826_presupuestos_externo.sql` en Supabase. Después, repetir la simulación de insert `tipo:'externo'` (o simplemente probar el botón real en `/g`) para cerrar el punto 2 del todo.
- Probar el modo "Por etapas" del presupuestador embebido con un caso real en producción (ahí sí funcionan las Cloudflare Functions) para confirmar que la IA agrupa y el guardado final con `user_id: null` se ve bien de punta a punta con datos reales.
- Confirmar visualmente en producción (celular de Gustavo, y `/admin` con la contraseña real) que la navegación a cards se ve y funciona como se espera — Admin.tsx quedó sin poder probarse en este entorno.

## Sesión 26/08/2026 (continuación 2) — Comprobantes de pago semanal, ficha de cliente (facturación+marketing), `.env.example` — implementado siguiendo `steady-purring-spring.md`

Implementado en el orden del plan aprobado, `tsc --noEmit` limpio después de cada paso y al final.

1. **`.env.example` — hecho.** Nuevo archivo en la raíz con los nombres de variable (sin valores), mismo agrupamiento/comentarios que `.env`, más `GOOGLE_SERVICE_ACCOUNT_JSON`/`CONTROL_OBRA_SHEET_ID` marcadas como solo-producción/Cloudflare. Sin lógica, sin riesgo.

2. **Comprobantes de pago semanal — construido, bloqueado en una migración pendiente de Alexandra.** `sql/20260826_pago_semanal_comprobantes.sql` (tabla `pago_semanal_comprobantes`, mismo patrón anon-full-access de siempre). `functions/api/parse-comprobante.js` nuevo (mismo esqueleto que `parse-factura.js`, prompt para `{monto, fecha, destinatario, banco, numero_operacion}`). En `PanelPagoSemanal` (`src/components/PanelesObra.tsx`) cada fila de trabajador de la semana ahora tiene una columna "Comprobante" con botón "+ Comprobante": sube la captura al bucket `audio-notas`, llama a `/api/parse-comprobante`, inserta en `pago_semanal_comprobantes` con `monto_calculado` = el `total` de esa fila. Si `monto_leido !== monto_calculado` (redondeado a peso) se muestra un badge rojo "⚠ No coincide: comprobante $X · calculado $Y" enlazando a la captura, sin bloquear nada; si coincide, un link verde "✓ Coincide"; si la IA no pudo leer el monto, un link neutro "Captura sin monto leído". **Verificado en navegador (Vite local + Playwright) contra el panel de Gustavo real:** la tabla de la semana en curso muestra 5 filas reales (Fabriel/Henry/Manuel/Misael/Samuel), coincidentes exacto con el cálculo hecho a mano contra Supabase (`reportes_diarios` × `trabajadores`, semana 24-30 ago: $30.000/$120.000/$135.000/$105.000/$90.000, total $480.000 — coincide con lo que ya calculaba `PanelPagoSemanal` antes de este cambio, no se tocó esa lógica). El botón "+ Comprobante" sube la imagen a Storage sin error; la llamada a `/api/parse-comprobante` falla con 404 bajo Vite (esperado, Cloudflare Functions no corren en local) y cae en el aviso "no pudo leerla, se guarda sin monto leído"; el insert final falla con el error esperado `Could not find the table 'public.pago_semanal_comprobantes'` — confirma que el código está listo y solo falta la migración. **No se pudo ver el badge "✓ Coincide"/"⚠ No coincide" renderizado con datos reales** porque eso requiere la tabla creada (para el insert) y, para leer `monto_leido` de verdad, la función en producción (Cloudflare) — ambas cosas pendientes. Archivo de prueba subido a Storage borrado después del test.

3. **Ficha de cliente (facturación + marketing) — construida, bloqueada en dos migraciones pendientes de Alexandra, con un hallazgo real importante encontrado y corregido en el camino.**
   - `sql/20260826_clientes_facturacion.sql`: agrega a `clientes` — `razon_social`, `giro`, `direccion_fiscal` (facturación), `origen`, `notas` (marketing), todas nullable. Tipo `Cliente` en `src/types/index.ts` extendido con los mismos campos. Confirmado contra Supabase real (vía MCP, `list_tables`) que `clientes` hoy solo tiene las columnas viejas — la migración es necesaria, no cosmética.
   - **Componente compartido nuevo `PanelClientes` en `src/components/PanelesObra.tsx`**: lista de clientes desde `clientes` (nombre + rut), detalle con dos secciones editables inline ("Facturación": rut/razón social/giro/dirección fiscal; "Marketing": origen/notas) guardadas con `update` directo por `id`, más el timeline de pendientes que ya existía (mismo join por nombre de antes). Reemplaza el `PanelClientes` local de `Gustavo.tsx` (borrado) y la sección `clientes` de `Admin.tsx` (que antes era una vista distinta, con resumen de interacciones/vencidos, "Historial", "+ Pendiente" y "Archivar"). Para no perder esas dos acciones que ya existían solo del lado de Admin, `PanelClientes` acepta `modoAdmin`/`onNuevoPendiente` opcionales (default apagado): con `modoAdmin` muestra un toggle "Ver archivados" y el botón Archivar/Desarchivar en el detalle; `onNuevoPendiente` conecta el botón "+ Pendiente" del detalle al mismo `abrirSeguimiento` que ya usaba Admin. Gustavo.tsx sigue llamando `<PanelClientes />` sin esos props (mismo comportamiento simple de antes, más las secciones nuevas). **Esta decisión de agregar `modoAdmin`/`onNuevoPendiente` no estaba escrita en el plan** (el plan solo describía lista+detalle+timeline) — se agregó para no borrar silenciosamente dos funciones ya en uso diario en Admin (archivar clientes viejos, atajo para crear un pendiente) que el plan no mencionaba que se fueran a eliminar. Queda para que el Estratega/Alexandra confirmen que este alcance ampliado es el correcto.
   - **Hallazgo real que cambiaba el alcance, encontrado antes de cerrar esto (verificado contra Supabase, no asumido):** el plan asumía que la tabla `clientes` tenía más cobertura que la lista derivada de `pendientes.cliente_nombre` ("un cliente que existe en `clientes` pero nunca tuvo un pendiente no aparece en ninguna lista"). Al verificar contra los datos reales antes de dar esto por bueno, resultó ser al revés: `clientes` tenía solo 2 filas, **ambas restos de pruebas de sesiones anteriores** (`Cliente Prueba Playwright` del 25/08, `PRUEBA PLAYWRIGHT EXTERNO` del 26/08 — de las pruebas de "presupuesto externo" de la sesión pasada, nunca limpiadas), mientras que `pendientes.cliente_nombre` tiene **31 nombres de clientes reales distintos**, ninguno con fila en `clientes`. Si se pasaba la lista a leer solo de `clientes` sin corregir esto, el panel de Clientes de Alexandra y Gustavo hubiera pasado de mostrar 31 clientes reales a mostrar 2 filas de prueba — una regresión seria de un día para otro, no lo que se pidió. Corregido con: (a) se borraron las 2 filas de prueba huérfanas (confirmado antes que no tenían ningún `pendiente`/`presupuesto` enganchado por `cliente_id`, usando la anon key del propio proyecto, mismo canal que usa la app — el MCP es de solo lectura); (b) migración nueva `sql/20260826_clientes_backfill_desde_pendientes.sql`, solo-agregar (`insert ... on conflict (nombre) do nothing`), que carga en `clientes` los 31 nombres reales que hoy solo existen en `pendientes`. Con las 3 migraciones corridas en el orden `clientes_facturacion` → `clientes_backfill_desde_pendientes` → (la de pago semanal es independiente), el panel de Clientes debería mostrar los 31 clientes reales de siempre, ahora editables con RUT/facturación/marketing.
   - **Verificado en navegador (Vite local + Playwright):** antes del backfill/borrado de prueba, la lista mostraba exactamente esas 2 filas de prueba (confirma el hallazgo de arriba visualmente, no solo por SQL). El detalle de cliente abre bien, muestra las 6 secciones editables (RUT, Razón social, Giro, Dirección fiscal, Cómo llegó, Notas) y "Sin historial" para el cliente sin pendientes. Se probó editar Notas/Giro y guardar: falla con el error esperado `Could not find the 'direccion_fiscal' column of 'clientes'` — confirma que el código está listo y solo falta correr `sql/20260826_clientes_facturacion.sql`. **No se pudo probar el guardado exitoso de punta a punta ni ver un cliente real en la lista** porque ambas migraciones (`clientes_facturacion` y el backfill) siguen pendientes — queda para la próxima verificación después de que Alexandra las corra.
   - **Admin.tsx no se pudo probar en vivo** (login vía Cloudflare Function, límite conocido de siempre) — se verificó por lectura de código y `tsc --noEmit` limpio, apoyado en que usa exactamente el mismo `PanelClientes` ya probado del lado de Gustavo, con `modoAdmin`/`onNuevoPendiente` conectados a las funciones que ya existían (`abrirSeguimiento`). Se quitaron de Admin.tsx el estado y las funciones que quedaron sin uso al migrar (`clientes`/`verArchivados`/`loadClientes`/`archivarCliente`/`clientesSummaryTodos`/`clientesArchivadosCount`/`clientesSummary`) — confirmado que no se usaban en ningún otro lado del archivo antes de borrarlos (`tsc` los hubiera marcado como error si algo los siguiera necesitando, y no fue el caso).

**Pendiente para la próxima sesión / para Alexandra — 3 migraciones a correr, en este orden sugerido:**
1. `sql/20260826_clientes_facturacion.sql` (agrega columnas a `clientes`).
2. `sql/20260826_clientes_backfill_desde_pendientes.sql` (carga los 31 clientes reales que hoy solo están en `pendientes` — sin esto el panel de Clientes se ve casi vacío).
3. `sql/20260826_pago_semanal_comprobantes.sql` (tabla nueva, independiente de las dos anteriores).

Después de correr las 3: repetir la prueba de guardar Facturación/Marketing de un cliente real y confirmar contra Supabase que quedó; subir un comprobante real desde "Pago semanal" en producción (ahí sí corre `/api/parse-comprobante`) y confirmar que el badge de coincide/no-coincide se ve bien con un monto leído real por la IA — ninguna de las dos cosas se pudo verificar de punta a punta todavía porque dependen de piezas que no existen en este entorno local (las tablas nuevas y las Cloudflare Functions).

**Nota aparte, ya corregida esta sesión, no bloqueante:** se encontraron y borraron 2 filas de prueba huérfanas en `clientes` (`Cliente Prueba Playwright`, `PRUEBA PLAYWRIGHT EXTERNO`) dejadas por sesiones anteriores sin limpiar — señal de que conviene revisar al cierre de cada sesión si quedó algún dato de prueba sin borrar en tablas nuevas, no solo en las que se tocaron ese día.

## Para la próxima sesión — empezar acá
1. **Leer este archivo y `progress/propuesta_arquitectura_operativa.md` primero** (regla del arnés, `CLAUDE.md`).
2. **Bloqueador activo, primera cosa a resolver mañana:** Alexandra tiene que entrar a Cloudflare Pages → proyecto `horma-presupuestos` → **Settings → Environment variables** → agregar en **Production** la variable `VITE_PRESUPUESTO_TOKEN` con el valor `91f471c31d2b2c9b` (mismo valor que ya está en `.env` local) → guardar → **disparar un redeploy** (agregar la variable sola no reconstruye lo ya publicado; se puede forzar con "Retry deployment" sobre el último deploy, o pedir acá un commit vacío para gatillar uno nuevo). Sin esto, el link "Presupuesto" de Admin sigue roto en producción aunque el código ya esté arreglado.
3. **Después de que Alexandra confirme que agregó la variable: volver a comparar el bundle de producción** (mismo método que se usó para diagnosticar — bajar `https://horma-presupuestos.pages.dev/` y el JS que referencia, buscar el token `91f471c31d2b2c9b` en el `href` real) antes de dar el bug por cerrado. No asumir que "ya lo agregué" alcanza — confirmar contra el bundle real, mismo criterio que se usa siempre acá para no confiar solo en la palabra de que algo se corrió.
4. **Nivel 1, Nivel 2, la "visión ampliada" (2.7-2.13), Nivel 3 pasos 1-3 de 4, y la mejora de vistas Día/Semana/Mes + Maps/Waze del calendario están construidos y verificados de punta a punta contra Supabase real** — no hay nada pendiente de esas secciones.
5. **Solo queda Nivel 3, paso 4 (alerta de material faltante), bloqueado hasta 2.2/3.2** (desglose `obra_items`) — no arrancarlo sin esa base. Ver "Plan acordado para Nivel 3, en 4 pasos" en `propuesta_arquitectura_operativa.md`.
6. Repositorio: todos los commits de hoy (calendario, orden de pestañas, fix del link) están pusheados a `main` — confirmar que no queda nada suelto sin subir antes de asumir que algo nuevo está en producción.

## Sesión 25/08/2026 (continuación) — MCP arriba: 0.2 hecho, hallazgo extra (0.1 ya estaba hecho), 1.1 construido y probado

Sesión reiniciada específicamente para que el MCP `supabase-horma` quedara autorizado desde el arranque (ver entrada anterior de hoy — autorizar a mitad de una sesión abierta no sirve). Confirmado: `ToolSearch` encontró `mcp__supabase-horma__*` de entrada, sin fricción.

- **0.2 (verificar esquema real de `presupuestos`) — hecho.** Vía `list_tables`/`pg_policies` (solo lectura): la tabla existe con las columnas que ya usaba `PresupuestoEtapas.tsx`, `user_id` es nullable a nivel de columna, pero **las políticas RLS solo cubren el rol `authenticated`** (INSERT con `auth.uid()=user_id`) — no hay policy para `anon`. Un insert desde el presupuesto simple (sin login, solo token de URL) queda bloqueado por RLS tal cual estaba.
- **Hallazgo extra, no esperado: 0.1 ya estaba hecho.** El backlog decía "`clientes.sql` escrita pero no ejecutada" — pero la tabla `clientes` ya existe en Supabase con su policy `anon full access`, esquema idéntico al SQL del repo. Alexandra debe haberla corrido sin que quedara anotado. Corregido en `propuesta_arquitectura_operativa.md`.
- **1.1 (presupuesto simple guarda en Supabase con `cliente_id`) — construido y probado, bloqueado en un paso de Alexandra.**
  - `sql/presupuestos_migracion.sql` nuevo (mismo flujo que `clientes.sql`): agrega `cliente_id` (FK a `clientes`), `referencia` (el ID `HRM-XXXXX`, hasta ahora solo client-side), `items` jsonb, `tipo` (`'simple'|'etapas'`) y la policy `anon full access` — siguiendo el mismo patrón de confianza que `pendientes`/`obras`/`reportes_*`/`clientes` (documentado en `decisiones.md`), no un diseño nuevo.
  - `Presupuesto.tsx`: al generar el PDF, hace upsert de `clientes` por nombre (sin pisar RUT/email existentes si el campo viene vacío) y guarda en `presupuestos` con `tipo:'simple'`. Falla silenciosa (solo consola) si Supabase no responde — no bloquea nunca la generación del PDF, mismo patrón que `PresupuestoEtapas.tsx`.
  - **Probado en navegador real, dos pasadas.** Primera pasada (antes de la migración): PDF se generó bien y el guardado falló con el error exacto esperado (`PGRST204 — Could not find the 'cliente_id' column`), confirmando que el código estaba listo. **Alexandra corrió la migración en el momento** — segunda pasada (después): mismo flujo, sin error en consola. Verificado en Supabase (vía MCP, solo lectura): fila real con `cliente_id` correctamente vinculado a un `clientes.nombre` (el upsert creó el cliente), `items` jsonb con el ítem cargado, `referencia`/`subtotal`/`iva`/`total` correctos, `tipo='simple'`. Datos de prueba borrados después (el MCP no puede hacer DELETE — se usó la anon key de la propia app, mismo canal que usa el frontend, para limpiar).
  - `tsc --noEmit` limpio, `init.sh` en verde.
  - **1.1 cerrado del todo.**
- **1.2 (trabajos puntuales suman a Estado de Resultados) — construido, bloqueado en Alexandra corriendo `sql/trabajos_puntuales_monto.sql`.** Campo `monto` nuevo en el form de Reporte Diario, `PanelEstadoResultados` ya lo suma a Ingresos del mes (solo consolidado, no tiene `obra` asociada). `tsc`/`init.sh` en verde. **Hallazgo real:** ya hay una fila (21/08, Friburgo 5007) con "Total 91.630" en la descripción que nunca se contó — pendiente que Alexandra cargue ese monto real después de correr la migración.
- **Ideas nuevas de Alexandra, agregadas al backlog como 2.5 y 2.6** (no implementadas todavía): galería de fotos/videos por obra/cliente en la app (reemplaza Drive) y calendario compartido de disponibilidad entre Gustavo/Alexandra/trabajadores para no pisar una visita técnica con otra. Recomendación dada: la galería reusa el patrón de Storage que ya existe (bajo esfuerzo); el calendario mejor arrancarlo liviano (sin login real todavía) en vez de esperar 4.1.
- **Nivel 1 (1.3, 1.4) — sin empezar**, no dependen de nada nuevo, se puede retomar directo la próxima sesión.

## Sesión 25/08/2026 — Retomando el backlog de `propuesta_arquitectura_operativa.md`: 0.3 hecho, 0.2 bloqueado por MCP

Alexandra pidió seguir con 0.2, 0.3 y después Nivel 1 del backlog nuevo (`progress/propuesta_arquitectura_operativa.md`, escrito la sesión anterior 24-25/08).

- **0.3 (proteger la ruta `/`) — hecho y verificado.** Mismo patrón token-en-URL que `/g`/`/reporte`: nuevo `VITE_PRESUPUESTO_TOKEN` en `.env`, `App.tsx` le pasa `token` a `Presupuesto`, pantalla "Link inválido" si no coincide. Probado en navegador real con Playwright headless (no había `chromium-cli` instalado, se instaló Playwright suelto en el scratchpad): sin token bloquea, con token correcto muestra el formulario, con token incorrecto bloquea, cero errores de consola. `tsc --noEmit` limpio, `init.sh` en verde.
  - **Pendiente que solo puede hacer Alexandra:** agregar `VITE_PRESUPUESTO_TOKEN=91f471c31d2b2c9b` a las variables de entorno de Cloudflare Pages (si no, en producción nadie va a poder entrar a `/`) y mandarle a quien use esa pantalla el link con `?t=91f471c31d2b2c9b`.
- **0.2 (verificar esquema real de `presupuestos`) — bloqueado por MCP, no llegó a hacerse.** El MCP `supabase-horma` (`.mcp.json`, `read_only=true`, project-ref `fhcebphnvnozaxherpbf`) no estaba autorizado en esta sesión. Alexandra lo autorizó a mitad de sesión "vía PowerShell", pero **la sesión de Claude Code ya estaba corriendo y no recogió la conexión en caliente** — se probó `ToolSearch` varias veces después de la autorización y las herramientas `mcp__supabase-horma__*` seguían sin aparecer. Conclusión: la conexión MCP se establece al arrancar la sesión, autorizar a mitad de una sesión abierta no alcanza. **Alexandra decidió reiniciar la sesión** para que levante ya autorizada.
  - Lo que se pudo sacar del código sin la DB (para retomar 0.2 rápido): `PresupuestoEtapas.tsx:266-278` inserta en `presupuestos` mandando `user_id: session.user.id`, es decir depende de un login real de Supabase Auth. El presupuesto simple (`/`, sin login) no tiene ese `user_id` — antes de hacer que también guarde ahí (tarea 1.1) hay que confirmar contra Supabase real si `user_id` es `NOT NULL` y qué política RLS tiene la tabla, porque un insert anónimo puede fallar directo. No se puede adivinar tratándose de datos que van a Estado de Resultados/Power BI.
  - **Nota importante:** existe otro MCP ya autenticado en esta sesión, `pyme-cumplimiento` — es un proyecto Supabase totalmente distinto (`jnnsztnmpgularecysii.supabase.co`, confirmado con `get_project_url`), **no es Horma**. No usarlo por error para nada de este proyecto.
- **Nivel 1 (1.1 a 1.4) — sin empezar**, a la espera de que la sesión reiniciada tenga el MCP arriba para completar 0.2 primero (1.1 depende directo de eso).

## Sesión 22/08/2026 — Auditoría del arnés (harness engineering), solo capa de configuración
Auditoría del arnés del proyecto contra el checklist de `harness_engineering.md` (no se tocó `src/`, `functions/`, `sql/` ni lógica de la app). Detalle completo en `decisiones.md` (2026-08-22) — resumen: subagents migrados de `agents/*.md` (texto plano) a `.claude/agents/*.md` con frontmatter real, `SISTEMA.md`/`TAREA_REPORTE_DIARIO.md` marcados como archivados (contenido ya completado / contradecía el deploy real), primer hook del proyecto agregado (`check_git_push_account.py`, avisa si el remote pierde la cuenta correcta antes de un `git push`, fail-open probado a mano). `init.sh` corrido después de cada cambio — sigue en exit 0, `tsc --noEmit` limpio.

## Sesión 20/08/2026 — Pago semanal, Cuentas por cobrar unificadas, sync Sheets ampliado

- **Refactor grande de la sesión del 18/08 (12+ commits) por fin committeado y pusheado** — llevaba 2 días solo en el disco local. Causa del bloqueo de push: Windows tenía 3 cuentas de GitHub cacheadas y usaba por defecto la incorrecta (`alexandra-albarracin`, sin permiso); se resolvió apuntando el remote a `hormaelectricidadcl-cpu` explícitamente.
- **Pestaña nueva "Pago semanal"** (`PanelPagoSemanal` en `PanelesObra.tsx`, en Admin y Gustavo): tabla Nombre/Días/Ganado/Viático/Total por trabajador, cruzando todas las obras, con selector de semana y auto-refresco cada 20s. Fabriel (sueldo fijo, sin tarifa diaria) se marca aparte, solo se le calcula viático. Verificado contra Supabase con datos reales.
- **Cuentas por cobrar dividida en Pendientes/Cobradas** (calculado, no manual — se autocorrige si se agrega o quita un abono). Verificado: las 2 pendientes reales (Doctora Eloísa 5860 $450.000, Luis Carrera $1.815.000) coinciden exacto con lo que Gustavo reportó por WhatsApp el 17/08.
- **Cuentas por cobrar unificada con las obras de presupuesto fijo**: Ohiggins 126 Limache y Luz 2979 se rastreaban solo en la pestaña Obras (nunca tuvieron una cuenta manual cargada) — ahora también aparecen ahí, de solo lectura, sin duplicar.
- **Reconciliación completa contra el WhatsApp de Gustavo del 17/08**: todo lo que dijo ya estaba correctamente cargado en Supabase — no había ningún dato faltante, el problema era solo de visibilidad (dos sistemas de cuentas por cobrar separados que no se mostraban juntos).
- **Sync a Google Sheets ampliado más allá de "Horas"**: 3 funciones nuevas (`sync-compras.js`, `sync-cobros.js`, `sync-subcontratos.js`, mismo patrón que `sync-horas.js`), todas solo-agregar (nunca borran/pisan filas — la planilla tiene entradas manuales viejas con columnas completadas a mano). `sync-compras.js` escribe en dos hojas a la vez (`Gasto en Materiales` + `Asignación Materiales`) porque la columna "Sobrante" de la primera depende de una fila espejo en la segunda vía SUMIF por Factura N° — sin eso, cualquier compra nueva se vería como material sin asignar. **No se pudo probar en vivo** (no hay wrangler/pages dev, solo vite) — pendiente que alguien cargue un reporte real y confirme que la fila aparece en la planilla.
- **Backfill manual ya hecho esta sesión** (vía MCP de Google Sheets, verificado): 4 compras (14–19/08) y 5 cobros de Ohiggins (17–18/08) que faltaban en la planilla desde antes de que existieran las funciones de sync.
- **Arnés del proyecto construido** (antes solo existía `CLAUDE.md` desactualizado + `estado_actual.md`): `CLAUDE.md` reescrito con las reglas reales (verificación, límites conocidos, dinero real), `progress/decisiones.md` y `progress/tareas.md` nuevos, `agents/estratega.md`/`constructor.md`/`revisor.md` nuevos (adaptados de `E:\ALEXANDRA TRABAJO`, con la regla extra de verificar contra Supabase antes de aprobar cualquier cambio de dinero), `init.sh` nuevo — probado en vivo, corre `tsc --noEmit` de verdad además de chequear que los archivos existan. Exit code 0 confirmado.
- **8 anomalías reportadas por Alexandra en la pestaña Obras, todas revisadas contra Supabase y resueltas:**
  1. Bug real y el más serio de la tanda: **"Cobrado"/"Saldo" de una obra ignoraba la plata cargada vía el sistema manual de cuentas_por_cobrar.** Doctora Eloísa 5860 mostraba Cobrado $0 / Saldo -$753.528 cuando en realidad hay $7.050.000 cobrados y el saldo real es +$6.296.472. Corregido y verificado exacto.
  2. **`PanelObras` estaba duplicado** — versión completa en Gustavo.tsx, versión vieja/recortada embebida en Admin.tsx (sin Nueva obra/editar cliente/facturado). Se extrajo a `PanelesObra.tsx` compartido — cierra el pendiente abierto desde el 18/08.
  3. **Obras "En curso"/"Culminadas" nuevo** (reutiliza `obras.activa`, ver decisiones.md) con botón para marcar/reactivar. Solo se marcó Renato Sanchez como culminada (confirmado $0 pendiente); Luis Carrera 2700 sigue en curso porque su cuenta real todavía debe $1.815.000 — la premisa de que "ya se pagó completo" no coincidía con los datos, quedó aclarado.
  4. Cuentas por cobrar (obras): agregado Facturado/Por facturar, y ya no filtra por `activa` (una obra culminada puede seguir debiendo plata).
  5. Estado de Resultados: el selector de obra no leía la tabla `obras` ni `cuentas_por_cobrar`, le faltaban Renato Sanchez y Luz 2979. Corregido.
  6. Guía "¿Cómo se lee esto?": se agregaron los pasos de Facturado/Por facturar (faltaban) y se aclaró que Saldo es posición de caja, no lo mismo que "cuánto falta que pague el cliente".
  7. **Sin resolver, no es un bug:** a "Doctora Eloísa (dirección 5843)" le falta cargar su presupuesto total real — no se puede inventar el número, pendiente pedírselo a Gustavo.
  - Todo verificado en vivo contra Supabase (panel de Gustavo) antes de subir, `init.sh` corrido, tsc limpio.

## Sesión 20/08/2026 (continuación) — Rediseño de raíz, merge de pestañas, terminología, confianza en los datos

**Esta fue la parte más importante de la sesión — no parches, sino la causa raíz.** Alexandra notó que llevábamos ~2 horas en bucle arreglando síntomas del mismo problema y pidió explícitamente parar y rediseñar. Detalle completo del razonamiento en `decisiones.md` — acá el resumen de lo que quedó hecho:

- **"Falta por cobrar" dejó de mostrar dos números distintos en dos pestañas** (bug encontrado por Alexandra: Cuentas por cobrar y Obras daban cifras diferentes para la misma obra). Se agregó "Falta por cobrar" a la tarjeta de Obras, calculado exactamente igual que en Cuentas por cobrar.
- **Rediseño real de la causa raíz**: dos caminos independientes para cargar "el cliente pagó" (`obras.presupuesto_total`+`reportes_cobros` automático vs `cuentas_por_cobrar`+`abonos_cuenta` manual) sin ningún cruce entre ellos. Sin acceso a DDL desde acá (la API REST de Supabase no crea tablas), se migró Ohiggins y Luz 2979 al sistema manual (ya el más flexible) en vez de crear un esquema nuevo. `Reporte.tsx` ahora enruta cobros nuevos automático a la cuenta correcta cuando la obra tiene una sola cuenta, con aviso de posible duplicado antes de guardar (probado en vivo).
- **Se encontró y corrigió un cobro duplicado real de $2.000.000 en Luis Carrera 2700** — el mismo pago cargado dos veces (una como `reportes_cobros`, otra como 2 abonos de $1M) en la misma sesión de carga del 15/08. Verificado y corregido.
- **"Presupuesto" de una obra con cuentas ahora suma esas cuentas** en vez de usar un campo suelto que podía quedar desactualizado (pasó con Luis Carrera: mostraba $2.722.500 al lado de "Falta por cobrar $1.815.000" — contradictorio; el presupuesto real era $4.831.150).
- **Merge de "Obras" + "Cuentas por cobrar" en una sola pestaña** (pedido explícito de Alexandra: dos pestañas mostrando resúmenes de la misma obra confundía aunque los números coincidieran). `PanelCuentasPorCobrar` se eliminó del todo. El manejo de cuentas/abonos vive ahora dentro de "Detalle" de cada obra (soporta más de una cuenta por obra — Luis Carrera tiene 3). Cuentas sin obra (ej. "Visita Técnica") quedan en una sección aparte al final, no pestaña propia.
- **Terminología unificada a "Facturado"**: para Gustavo, cobrado/facturado/abonado son la misma cosa (plata que le dieron). "Cobrado"→"Facturado", "Falta por cobrar"→"Por facturar", "Abonado"/"Restante" en las cuentas también. Se eliminó la sección "Facturación formal" (tabla `facturas`) de Detalle porque competía con el mismo número usando la misma palabra para dos cosas distintas — los datos siguen en Supabase, solo se sacó de la interfaz.
- **Tarjeta de Obras reordenada como una historia** (pedido de Alexandra): Presupuesto → Facturado → Por facturar → Mano de obra → Compras → Subcontratos → Adelantos → Pagos semana → Saldo. "Presupuesto" ahora también es una card, no solo texto.
- **Ohiggins: se confirmó que el 5º cobro ($9.458.056, "Alejandra", 18/08) era duplicado** — Alexandra ya lo había avisado y, al no procesarse a tiempo, lo borró ella misma. Quedó documentada la lección: no dar una instrucción puntual por resuelta investigando algo parecido sin confirmar que es lo mismo.
- **Luz 2979: se migró su factura vieja ($4.733.671, tabla `facturas`) a abono real** — quedó huérfana al sacar la sección de Facturación formal. Verificado antes: Ohiggins tenía la misma plata en dos lados (no se tocó, migrarla habría duplicado); Luz 2979 no, era plata real sin cargar.
- **Conversación larga sobre seguridad de los datos** (Alexandra preguntó si esto es seguro, si necesita mantener Google Sheets, si puede ver todo en Supabase directo). Quedó claro: todo vive en Supabase (la app es solo una ventana), Sheets es un espejo de un solo sentido no un respaldo, Supabase tiene Table Editor para ver todo sin pasar por la app ni por mí, Power BI podría conectarse directo a Postgres como alternativa a Sheets. **Pendiente sin confirmar: si el proyecto tiene el plan Pro de Supabase con backups automáticos activados** — se ofreció revisarlo, no se llegó a hacer.

## Pendiente / hallazgo sin resolver
- **Confirmar el plan de Supabase (Free vs Pro) y si los backups automáticos están activos** — conversación de esta sesión, no verificado todavía. Prioridad alta dado que esto maneja plata real.
- No confirmado en vivo que `sync-compras.js`/`sync-cobros.js`/`sync-subcontratos.js` funcionen en producción — probar con una carga real y revisar la planilla.
- Falta el presupuesto real de "Doctora Eloísa (dirección 5843)" — pedírselo a Gustavo/Alexandra y cargarlo (recién ahí se puede migrar al sistema unificado).
- Anomalías de Google Sheets reportadas por Alexandra — pendientes de pasar y revisar en una sesión aparte (distinta fuente de verdad, distinta forma de verificar).
- Evaluar si Alexandra quiere una conexión de Power BI directa a Supabase (se lo ofrecí, sin decidir todavía).

## Sesión anterior — 14/08/2026

## Estado
Live en Cloudflare Pages (horma-presupuestos.pages.dev) — el `netlify.toml`/`netlify/functions` sigue en el repo pero el deploy real está en Cloudflare Pages, ojo con eso en sesiones futuras.
No se tocó `/api/parse` ni el modo IA de `ItemForm.tsx` — siguen en uso activo para presupuestos.

## Sesión 14/08/2026 — Reporte Diario de obra + puente a Google Sheets

- **Pantalla nueva `/reporte?t=TOKEN`** (`src/pages/Reporte.tsx`), mismo patrón de auth que `/g` y `/i`. Gustavo carga asistencia/obra/viático/adelanto por trabajador, compras, cobros, subcontratos y trabajos puntuales — reemplaza la interpretación manual de WhatsApp. Token: `VITE_REPORTE_TOKEN` (local `.env` y Cloudflare Pages).
- **Tablas nuevas en Supabase** (`sql/reportes_diarios.sql`, `sql/subcontratos_y_trabajos_puntuales.sql`): `reportes_diarios`, `reportes_compras`, `reportes_cobros`, `reportes_subcontratos`, `reportes_trabajos_puntuales`. RLS abierto a `anon`, mismo modelo de confianza que `pendientes` (el control de acceso lo hace el token de la URL).
- **Regla de negocio importante:** el viático solo corresponde a la obra de Limache (Ohiggins 126) — los equipos en obras de Santiago no lo reciben. Implementado en `Reporte.tsx` (`viaticoPorObra`, se autocompleta al elegir la obra, pero queda editable por si hay excepciones).
- **Bug real corregido:** el checkbox de "Viático"/"Ausente hoy" no mostraba feedback visual al tocarlo (el reset global `.pendientes input { appearance: none }` en `styles.css` afectaba también a los checkboxes). Se agregó una regla específica para `input[type="checkbox"]` que restaura la apariencia nativa — esto también destraba los checkboxes de `Admin.tsx` que ya usaban `accentColor` sin efecto visible.
- **Puente automático a Google Sheets construido:** `functions/api/sync-horas.js` (Cloudflare Pages Function). Cuando Gustavo guarda el Reporte Diario, además de Supabase, escribe automáticamente en la hoja "Horas" de "Control de Obra - Horma" (upsert por fecha+trabajador, respeta las fórmulas de tarifa/viático existentes). Probado de punta a punta con datos de prueba, funcionando en producción.
  - Autenticación con **cuenta de servicio de Google** (`horma-sheets-sync@horma-hermes.iam.gserviceaccount.com`), no login personal — evita el problema de tokens que caducan cada 7 días (causa raíz de un bloqueo real esta sesión: el token OAuth de `D:\HORMA LAPTOP\horma_sistema\horma hermes\google_token.json` había expirado por estar la app de Google en modo "Testing").
  - Variables de entorno nuevas en Cloudflare Pages: `GOOGLE_SERVICE_ACCOUNT_JSON` (contenido completo del JSON de la cuenta de servicio) y `CONTROL_OBRA_SHEET_ID`.
  - JWT firmado con Web Crypto API (compatible con el runtime de Cloudflare Pages Functions, sin `crypto` de Node).
  - Alcance actual: solo la hoja "Horas". Compras/cobros/subcontratos capturados en la app **todavía no** se sincronizan a "Gasto en Materiales"/"Cobrado"/"Subcontratos" — pendiente si se necesita.
- **Backfill manual de datos reales:** se completaron miércoles 12/8 a viernes 14/8 en Supabase y en la hoja "Horas" (Gustavo no había usado la pantalla el miércoles, y el checkbox roto de viático dejó el viernes con viático en falso para todos — corregido).
- **Hallazgo pendiente de resolver (no bloqueante):** el nombre de obra "Doctora Eloísa - Obra 1" no es idéntico entre la app (`(dirección 5860)`) y la hoja "Obras"/"Horas" (`(dirección pendiente) — CONFIRMAR CON GUSTAVO`) — no rompe los cálculos (las fórmulas de Horas buscan por trabajador, no por obra), pero convendría unificarlo antes de confiar en hojas que sí agrupan por obra (ej. "Rentabilidad por Obra").
- **Hallazgo aparte, sin resolver:** `GUSTAVO_WHATSAPP`/`ALEXANDRA_WHATSAPP` en `.env` apuntan al teléfono viejo de la sociedad anterior (9 5143 9958) — puede ser el celular personal real de Gustavo (uso interno, no público) o un descuido; no se tocó, queda flag para confirmar con Alexandra.

## Cómo alimenta el flywheel
Demuestra capacidad técnica full-stack → credibilidad ante agencias España

## Próxima acción
- Unificar el nombre de "Doctora Eloísa - Obra 1" entre la app y la Google Sheet.
- Evaluar si se necesita sincronizar compras/cobros/subcontratos a sus hojas correspondientes.
- Confirmar con Alexandra si el teléfono viejo en `GUSTAVO_WHATSAPP`/`ALEXANDRA_WHATSAPP` sigue vigente.
